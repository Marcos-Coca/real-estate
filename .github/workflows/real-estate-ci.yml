name: Real Estate CI

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Echo the message"
        run: echo "Hola marcos"
